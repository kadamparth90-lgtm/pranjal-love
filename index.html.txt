<!DOCTYPE html><html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Pranjal, Will You Be Mine Forever? 💖</title>  <style>
    body {
      margin: 0;
      font-family: 'Poppins', sans-serif;
      background: linear-gradient(135deg, #ff9a9e, #fad0c4);
      display: flex;
      align-items: center;
      justify-content: center;
      min-height: 100vh;
      color: #333;
      overflow: hidden;
    }

    canvas {
      position: fixed;
      top: 0;
      left: 0;
      pointer-events: none;
    }

    .card {
      background: white;
      padding: 30px;
      border-radius: 20px;
      box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
      max-width: 420px;
      text-align: center;
      animation: fadeIn 1s ease-in-out;
      z-index: 2;
    }

    h1 { color: #ff4b7d; }
    .hidden { display: none; }

    button {
      background: #ff4b7d;
      border: none;
      color: white;
      padding: 12px 20px;
      margin: 8px;
      border-radius: 12px;
      cursor: pointer;
      font-size: 16px;
      transition: 0.3s;
    }

    button:hover { background: #ff1f5a; transform: scale(1.05); }

    .photo {
      width: 100%;
      border-radius: 16px;
      margin: 15px 0;
      box-shadow: 0 6px 20px rgba(0,0,0,0.25);
    }

    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }

    .timeline { text-align: left; margin-top: 20px; }

    .moment {
      background: #fff0f5;
      padding: 10px 15px;
      border-radius: 12px;
      margin: 10px 0;
      animation: fadeIn 1.5s ease;
    }
  </style></head><body>
  <canvas id="fireworks"></canvas>  <div class="card"><div id="start">
  <h1>Hi Pranjal ❤️</h1>
  <p>I made something special for you…</p>
  <button onclick="showQuiz()">Tap to Begin 💫</button>
</div>

<div id="quiz" class="hidden">
  <h1>Little Love Quiz 💭</h1>
  <p>Who loves you the most?</p>
  <button onclick="nextStep()">Me ❤️</button>
  <button onclick="nextStep()">Only Me 😌</button>
</div>

<div id="timeline" class="hidden">
  <h1>Our Love Story 📖</h1>
  <img src="PHOTO_URL" class="photo" alt="Us together" />

  <div class="timeline">
    <div class="moment">✨ The day I first saw you… my world changed.</div>
    <div class="moment">💬 Every small talk with you became my favorite memory.</div>
    <div class="moment">😊 Your smile became my happiness.</div>
    <div class="moment">❤️ And today… you are my everything.</div>
  </div>

  <button onclick="finalProposal()">Continue 💌</button>
</div>

<div id="final" class="hidden">
  <h1>Pranjal… 💍</h1>
  <p>
    I don’t promise a perfect life… but I promise to hold your hand always.
    <br /><br />
    I don’t promise no tears… but I promise to wipe them.
    <br /><br />
    I just want one thing forever…
    <br /><br />
    <strong>Will you be mine for the rest of my life? ❤️</strong>
  </p>

  <button onclick="yesLove()">YES 💖</button>
  <button id="noBtn" onclick="moveNo()">NO 🙈</button>
</div>

<div id="result" class="hidden">
  <h1>Best Day of My Life 💕</h1>
  <p>I love you forever, Pranjal. ❤️</p>
</div>

  </div>  <script>
    function showQuiz() {
      start.classList.add('hidden');
      quiz.classList.remove('hidden');
    }

    function nextStep() {
      quiz.classList.add('hidden');
      timeline.classList.remove('hidden');
    }

    function finalProposal() {
      timeline.classList.add('hidden');
      final.classList.remove('hidden');
    }

    function yesLove() {
      final.classList.add('hidden');
      result.classList.remove('hidden');
      startFireworks();
    }

    function moveNo() {
      const btn = document.getElementById('noBtn');
      const x = Math.random() * 200 - 100;
      const y = Math.random() * 200 - 100;
      btn.style.transform = `translate(${x}px, ${y}px)`;
    }

    // Fireworks animation
    const canvas = document.getElementById('fireworks');
    const ctx = canvas.getContext('2d');
    let particles = [];

    function resizeCanvas() {
      canvas.width = window.innerWidth;
      canvas.height = window.innerHeight;
    }
    window.addEventListener('resize', resizeCanvas);
    resizeCanvas();

    function startFireworks() {
      setInterval(createFirework, 500);
      animate();
    }

    function createFirework() {
      const x = Math.random() * canvas.width;
      const y = Math.random() * canvas.height / 2;
      for (let i = 0; i < 50; i++) {
        particles.push({
          x, y,
          angle: Math.random() * Math.PI * 2,
          speed: Math.random() * 5 + 2,
          life: 100
        });
      }
    }

    function animate() {
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      particles.forEach((p, i) => {
        p.x += Math.cos(p.angle) * p.speed;
        p.y += Math.sin(p.angle) * p.speed;
        p.life--;

        ctx.beginPath();
        ctx.arc(p.x, p.y, 2, 0, Math.PI * 2);
        ctx.fillStyle = `hsl(${Math.random() * 360},100%,60%)`;
        ctx.fill();

        if (p.life <= 0) particles.splice(i, 1);
      });
      requestAnimationFrame(animate);
    }
  </script></body>
</html>
